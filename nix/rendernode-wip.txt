# CANNOT USE THIS IN THE SAME DERIVATION AS AUTOPATCHELF WILL MIX BOTH APPS WHICH IS BAD

# render="$out/opt/FusionRender${major}"
# if [ -e Blackmagic_Fusion_Render_Node_Linux_${version}_installer.run ]; then
#   appimage-exec.sh -x "$render" Blackmagic_Fusion_Render_Node_Linux_${version}_installer.run
#   rm $render/FusionInstaller
#   rm $render/FusionInstaller.desktop
#   rm $render/AppRun

#   cat $render/FusionRenderNode.desktop | sed -e "s|/opt/BlackmagicDesign/FusionRenderNode${major}|$render|" > $out/share/applications/FusionRenderNode${major}.desktop
#   rm $render/FusionRenderNode.desktop
#   mv $render/FusionRenderNode.directory $out/share/applications/FusionRenderNode${major}.directory

#   getBinWrapper FusionRender${major}/FusionRenderNode > $out/bin/fusion-render
#   ln -s $out/bin/fusion-render $out/bin/fusion${major}-render
# fi